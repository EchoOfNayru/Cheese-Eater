#pragma once

//returns the lesser of 2 numbers
int min(int a, int b);

//returns the greater of 2 values
int max(int a, int b);

//returns a value clamped within a range
int clamp(int lower, int upper, int value);

//returns the distance bewteen the 2 points
int dist(int x1, int y1, int x2, int y2);