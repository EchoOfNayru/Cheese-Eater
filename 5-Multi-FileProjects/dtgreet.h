#pragma once

// prints a greeting to console based on month/day/year
void dayGreeting(int month, int day, int year);

// prints a greeting to console based on hour/minutes
void timeGreeting(int hours, int minutes);

// returns true if year is a leap year (every 4th year)
bool isLeapYear(int year);